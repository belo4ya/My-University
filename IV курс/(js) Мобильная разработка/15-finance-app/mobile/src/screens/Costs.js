import React from 'react';
import {FlatList, ScrollView, Text, View} from "react-native";

const Costs = () => {
  return (
    <View>
      <ScrollView>
        <Text>Costs</Text>
      </ScrollView>
    </View>
  );
};

export default Costs;
